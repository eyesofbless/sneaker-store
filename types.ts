

export interface SearchParams {
    searchParams: {
        brands: string[]
        types: string[];
        genders: string[];
        seasons: string[];
        selectedOption: string;
        query:string
    }
}